import React from "react";
import PropTypes from "prop-types";

function AddChatButton({ children, className, id }) {
  return <div>AddChatButton</div>;
}

AddChatButton.propTypes = {};

export default AddChatButton;
